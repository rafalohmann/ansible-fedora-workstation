# Localhost for running playbooks locally (e.g., on your Fedora workstation)
all:
  children:
    local:
      hosts:
        localhost:
          ansible_connection: local
          ansible_become: true
